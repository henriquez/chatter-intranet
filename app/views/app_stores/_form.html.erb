<%= form_for(@app_store) do |f| %>
  <% if @app_store.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@app_store.errors.count, "error") %> prohibited this app_store from being saved:</h2>

      <ul>
      <% @app_store.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
